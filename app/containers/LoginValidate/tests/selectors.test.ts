// import { selectLoginValidateDomain } from '../selectors';

describe('selectLoginValidateDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
